# 考勤对账工具 · 工作流手册

> 本文档沉淀整个项目的运行工作流、开发历程与运维约定。
> 配套使用：`README.md`（安装/快速开始）、`config.json`（配置项）。

---

## 1. 工具定位

**输入**（三个源文件，放入 `inputfile/`，文件名随意）：

| 源 | 内容 | 说明 |
|---|---|---|
| 原始打卡记录 | xlsx，含打卡时间列 | 程序按表头自动识别列 |
| 手工考勤表 | xlsx，含 上/下/加/夜間工作 行标签 | 程序按"序號"定位日期列 |
| 单据 PDF | 请假/休假/加班/司机加班等 | 纯本地 OCR 解析 |

**输出**：`output/考勤对账差异报告_{周期}.xlsx`（7 个 sheet，硬错误 / 可疑项分级）。

---

## 2. 每月操作 SOP（日常核心流程）

### 2.1 三步跑完

```text
第1步  把当月三个源文件丢进 inputfile\ 目录（随意命名，可覆盖上月文件）
第2步  双击 启动考勤对账.bat
       （首次运行自动检测 Python / 依赖，缺失自动安装，需数分钟）
第3步  打开 output\ 下的差异报告，处理差异
```

### 2.2 报告怎么读

| 级别 | 含义 | 处理 |
|---|---|---|
| 硬错误 | 数据自相矛盾，必须修正 | 优先处理，逐条改考勤表或确认打卡 |
| 可疑项 | 需人工查证 | 按 sheet 分类核对，重点看备注 |
| 人工复核清单 | 手写单据 OCR 置信度低 | 打开原始 PDF 对应页人工确认 |

### 2.3 CLI 参数（可选）

```bat
python main.py                # 全流程
python main.py --skip-ocr     # 跳过 OCR（用上次缓存，最快）
python main.py --reocr        # 强制重新 OCR
python main.py --no-docs      # 仅阶段一（打卡×考勤表，不核对单据）
python main.py --report out.xlsx  # 指定报告输出路径
```

### 2.4 换月注意事项

- 不需要改 `config.json`：period 和 files 留空即自动检测
- 文件名任意：程序按文件名关键词 + 文件内容特征识别，`~$` 临时文件自动忽略
- 考勤周期：从文件名日期或打卡时间列自动推断；推断失败会明确提示
- 新入职员工简繁体差异：跑完看"姓名配对"sheet，未配对会自动给出模糊匹配建议，按建议补 `aliases.json` 即可

---

## 3. 三方对账逻辑

### 3.1 两条核对链

```text
原始打卡记录 ──┐
              ├──► 规则引擎 ──► 差异报告
手工考勤表   ──┘        │
              ▲        │
单据 PDF(OCR) ─────────┘
```

- 阶段一：打卡 × 考勤表（到场、缺卡、累计）
- 阶段二：单据 × 考勤表（假单、加班佐证）

### 3.2 五步处理流程

| 步骤 | 动作 | 关键逻辑 |
|---|---|---|
| 0 检测 | 缺文件提醒 + 自动识别源文件与周期 | 缺原始记录/考勤表→终止；仅缺 PDF→降级跑阶段一 |
| 1 读原始记录 | 解析打卡 | 表头别名自动识别列（简繁兼容），多格式日期 |
| 2 解析考勤表 | 员工块逐日解析 | "序號"行定位日期列，"月度累计"定位汇总列，每 4 行一组 |
| 3 规则引擎 | 姓名配对 + A1~A7 + 累计重算 | 见 3.3 |
| 4 OCR 三方核对 | 单据解析 + 单据×考勤表 | 8 类单据；"加班日期"关键字取日期（表头周期不作加班日期） |
| 5 报告 | 输出 7-sheet xlsx | 分级汇总 |

### 3.3 考勤规则口径

- 周一~五：全天上班，出勤记 √
- 周六：上午半天班，出勤记 √（单次打卡属正常，不报缺卡）
- 周日：休息日，√ 需有"休息日上班"数字佐证

**标记符号**：出勤 √（兼容 ✓/✔/V/v/对）；假别 曠/加/培/差/補/年/事/病/其；其 含 產/喪/婚（计入"其(d)"列）。

**规则组 A（到场核对，每人每天）**：

| 规则 | 触发 | 级别 |
|---|---|---|
| A1 | 有打卡但考勤表当天空白 | 硬错误（漏记） |
| A2 | 无打卡但上/下行标√ | 硬错误（多记/代打卡） |
| A3 | 有加班小时但当天无打卡 | 可疑 |
| A4 | 无打卡且标记假期类 | 可疑（待单据核对） |
| A5 | 周一~五仅 1 次打卡 | 可疑（缺卡） |
| A6 | 周日 √ 但无休息日上班数字 | 可疑 |
| A7 | 周六 √ 但无上午时段(6~14点)打卡 | 可疑 |

**规则组 B（累计重算）**：逐日标记求和 vs 考勤表"月度累计"列

- 假别标记出现一次 = 0.5 天；產/喪/婚 归入"其(d)"
- 数字按星期归类：周六→週六公眾假期上班(h)、周日→休息日上班(h)、其他工作日→待人工
- 比对 8 个天数列 + 4 个时数列（超時工作/休息日上班/週六公眾假期上班/夜間工作）
- 餐補計算 vs √ 天数 → 可疑（口径待确认）

> 已知案例：某员工丧假标记在原始表为繁体"喪"、未归一，导致"其(d)"列逐日求和=0 而月度累计=3。已修复为标记内容统一简体归一后再累计。

**姓名配对**：opencc 简繁归一 + aliases.json 手写字典 + 模糊匹配建议，输出 已配对 / 仅考勤表 / 仅原始记录。

---

## 4. 主要 Prompt 工作流（开发历程）

> 供后续继续开发/交接时参考：每个需求 → 对应产出。

| 阶段 | 需求要点 | 产出 |
|---|---|---|
| 方案 | 三方源文件，要自动化对比，先给思路 | 三方对账架构 + 三阶段路线 |
| 阶段一 | VSCode 建项目存 d:\attend，GitHub 管理 | 项目骨架 + 规则引擎初版 |
| 阶段一 | 简繁体配对统一 | opencc + 手写字典 |
| 阶段一 | 考勤表与打卡人数配对核对 | 姓名配对三态输出 |
| 阶段一 | 新增人员简繁问题易处理 | 未配对自动模糊匹配建议 |
| 阶段一 | 固化考勤规则（√/曠/加/培/差/補/年/事/病/其含產喪婚） | A1~A7 + 累计重算 |
| 阶段二 | PDF OCR 提取单据，三方核对，先打大版本 | rapidocr 本地 OCR + 8 类单据 + 单据核对 |
| 阶段二 | 表头周期 ≠ 加班日期，从"加班日期"取 | 字段提取修复（匹配 2→5，差异 489→324） |
| 阶段三 | 配置化 + 一键运行 | config.json 全配置驱动 |
| 阶段三 | 不改 period/files，自动读取 | auto_detect 自动识别源文件与周期 |
| 阶段三 | inputfile 目录约定，每月丢文件即可 | inputfile + 随意命名验证 |
| 可移植 | 简繁/路径/环境检查与修复 | 表头别名、多格式日期、缓存路径参数化 |
| 打包 | zip（不含数据源） | build_zip.py |
| 跨机 | 新电脑新源文件报错 | 表头自动识别列修复（不再硬编码列索引） |
| 启动 | bat 启动入口 + 自动装环境 | 启动考勤对账.bat |
| 提醒 | inputfile 无文件无提示 | 分场景缺文件提醒（仅缺 PDF 降级） |
| 修复 | GitHub 下载版 bat 闪退 | LF→CRLF 入库 + .gitattributes 固化 |

---

## 5. 常见问题排查

| 现象 | 原因 | 处理 |
|---|---|---|
| bat 双击一闪而过 | bat 被以 LF 行尾保存（cmd 无法解析） | 已用 `.gitattributes` 固化 CRLF；如自改过，用 CRLF 重新保存 |
| 提示缺少源文件 | inputfile 目录空或文件缺失 | 放入三类文件重跑；仅缺 PDF 时自动降级阶段一 |
| 无法确定考勤周期 | 文件名无日期且配置 period 为空 | 文件名带日期（如 20260721-20260820）或配置 period |
| 新电脑 IndexError | 源文件表头列序与程序预期不符 | 已修复为表头自动识别；若仍失败检查表头是否有"打卡时间"类列 |
| 手写单据无法识别 | 手写 OCR 精度限制（已实测多方案） | 维持 rapidocr：低置信度单据自动列入人工复核清单 |
| OCR 首次运行失败/慢 | rapidocr 首次需下载模型（几十 MB） | 联网跑一次即可缓存；完全离线环境需预置模型 |
| 依赖装不上 | 网络/版本问题 | 重跑 bat 自动重装；或手动 `pip install -r requirements.txt` |

---

## 6. 开发环境与依赖

- 本机：Windows / VSCode / Python 3.14.7 / Git / GitHub CLI
- 运行机：Windows + Python 3.10+（bat 自动安装 3.12）
- 依赖（requirements.txt，均轻量、无 pandas）：
  - openpyxl>=3.1（Excel 读写）
  - opencc-python-reimplemented>=0.1.7（简繁转换）
  - pymupdf>=1.23（PDF 解析）
  - rapidocr-onnxruntime>=1.2（本地 OCR，纯 CPU）
  - Pillow>=9.0（图像预处理）
  - numpy>=1.20（OCR 内部依赖）
- 测试：`python -m unittest tests/test_core.py`

---

## 7. Git 与发布约定

- 仓库：https://github.com/88xym/attend-check （公有）
- 分支：main；发布：打 tag（如 v1.0.0）+ GitHub Release
- **本机提交特殊说明**：本机安全软件拦截 git.exe 写对象文件（git 读操作正常），提交采用"Python 预写对象 + git add 更新索引 + Python 构建 commit + git push"方式；换机后正常 `git commit` 即可
- 换机同步：clone 后跑 `启动考勤对账.bat` 自动装环境；`aliases.json` 为本地文件（.gitignore 排除），换机需手动复制
- 打包：`python build_zip.py` → `attend-check.zip`（不含数据源与本地敏感文件）

---

## 8. 脱敏约定（重要）

仓库已公有，以下内容**禁止**提交：

- 任何员工真实姓名（源码、测试、文档、commit 信息）
- `aliases.json`（真实别名映射，本地维护，已被 .gitignore 排除）
- 源数据文件（打卡/考勤表/PDF）、输出报告、截图

提交前自查：`git status` 确认无上述文件；文档中用"员工甲/某员工"等占位表述。
